# Per-story personal design notes (kept locally, not shared)
docs/STORY-[0-9]*.md

# Local Claude Code config
.claude/settings.json

# E2E gate hook server logs
.e2e-gate/

# Build / test / tooling artifacts (never committed)
*.stackdump
test-reports/
.playwright-mcp/
