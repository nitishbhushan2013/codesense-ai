name: Deploy Backend

on:
  push:
    branches:
      - main
    paths:
      - backend/**
      - .github/workflows/deploy-backend.yml

jobs:
  build:
    name: Build & Test
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Set up Java 21
        uses: actions/setup-java@v4
        with:
          java-version: '21'
          distribution: temurin

      - name: Cache Maven packages
        uses: actions/cache@v4
        with:
          path: ~/.m2
          key: ${{ runner.os }}-m2-${{ hashFiles('backend/codesense-backend/pom.xml') }}
          restore-keys: ${{ runner.os }}-m2

      - name: Build
        run: |
          cd backend/codesense-backend
          ./mvnw clean package -DskipTests -q

      - name: Test
        run: |
          cd backend/codesense-backend
          ./mvnw test -q
        env:
          SPRING_PROFILES_ACTIVE: test

      - name: Upload JAR
        uses: actions/upload-artifact@v4
        with:
          name: backend-jar
          path: backend/codesense-backend/target/*.jar
          if-no-files-found: error

  deploy:
    name: Deploy to Azure App Service
    runs-on: ubuntu-latest
    needs: build
    environment: production

    steps:
      - name: Download JAR
        uses: actions/download-artifact@v4
        with:
          name: backend-jar
          path: ./artifact

      - name: Login to Azure
        uses: azure/login@v2
        with:
          creds: ${{ secrets.AZURE_CREDENTIALS }}

      - name: Deploy JAR to App Service
        run: |
          JAR=$(find ./artifact -name "*.jar" | head -1)
          echo "Deploying: $JAR"
          az webapp deploy \
            --resource-group codesense-rg \
            --name codesense-backend \
            --src-path "$JAR" \
            --type jar
